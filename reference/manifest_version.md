# Return the current version of the manifesto package

Return the current version of the manifesto package

## Usage

``` r
manifest_version()
```

## Value

A character version string.

## Examples

``` r
manifest_version()
#> [1] "0.0.1"
```
